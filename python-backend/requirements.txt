fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ML — install torch first with the right CUDA index URL
# GPU (CUDA 12.1):  pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU only:         pip install torch
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
peft>=0.18.1
